% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_omics.R
\name{prep_omics}
\alias{prep_omics}
\title{Preprocess exposures and multi-omics blocks for modeling}
\usage{
prep_omics(
  microbiome,
  metabolome,
  exposures,
  log1p_micro = TRUE,
  log1p_metab = TRUE,
  z_expo = TRUE,
  scale_omics = TRUE,
  na_action = c("error", "impute")
)
}
\arguments{
\item{microbiome}{Matrix/data.frame of samples x microbes.}

\item{metabolome}{Matrix/data.frame of samples x metabolites.}

\item{exposures}{Matrix/data.frame of samples x exposures.}

\item{log1p_micro}{If TRUE (default), apply log1p to microbiome.}

\item{log1p_metab}{If TRUE (default), apply log1p to metabolome.}

\item{z_expo}{If TRUE (default), z-score exposures.}

\item{scale_omics}{If TRUE (default), center/scale microbiome and metabolome features.}

\item{na_action}{What to do with NA values: "error" (default) or "impute".}
}
\value{
A list with processed matrices: \code{X}, \code{Y}, \code{E}.
}
\description{
Lightweight preprocessing for MVP and Bioconductor-friendly workflows.
Converts inputs to numeric matrices, checks sample alignment, optionally
imputes missing values, applies log1p transforms, and scales features.
}
\examples{
set.seed(1)
d <- generate_dummy_exporisk(n = 20, p_micro = 6, p_metab = 8, p_expo = 3)
al <- align_omics(d$microbiome, d$metabolome, d$exposures, d$meta,
                 id_col = "sample_id", strict = TRUE)
pr <- prep_omics(al$microbiome, al$metabolome, al$exposures)
str(pr)

}
