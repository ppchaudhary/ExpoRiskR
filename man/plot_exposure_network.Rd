% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_network.R
\name{plot_exposure_network}
\alias{plot_exposure_network}
\title{Plot exposure-adjusted multi-omics network (bipartite)}
\usage{
plot_exposure_network(
  net,
  file = NULL,
  width = 10,
  height = 7,
  dpi = 300,
  layout = "layout_with_fr",
  max_label_nodes = 30
)
}
\arguments{
\item{net}{A list returned by \code{build_exposure_network()} with elements
\code{$graph} and \code{$edges}.}

\item{file}{Optional output filename. If provided, saves a PNG (recommended).}

\item{width, height}{Plot device size (in inches) when saving.}

\item{dpi}{DPI when saving PNG.}

\item{layout}{Layout function name passed to igraph. Default
\code{"layout_with_fr"}.}

\item{max_label_nodes}{Max nodes to label (largest by degree). Default 30.}
}
\value{
Invisibly returns \code{net$graph}.
}
\description{
Plots a bipartite igraph network returned by \code{build_exposure_network()}.
Uses base igraph plotting (no extra dependencies).
}
\examples{
d <- generate_dummy_exporisk(seed = 1, n = 12, p_micro = 5, p_metab = 6, p_expo = 3)
al <- align_omics(d$microbiome, d$metabolome, d$exposures, d$meta,
                 id_col = "sample_id", strict = TRUE)
pr <- prep_omics(al$microbiome, al$metabolome, al$exposures)
net <- build_exposure_network(pr$X, pr$Y, pr$E, fdr = 0.95, max_pairs = 120, seed = 1)
plot_exposure_network(net)

}
